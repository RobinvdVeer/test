replicaCount: 1

image:
  app:
    repository: ghcr.io/robinvdveer/metrics-server
    tag: latest
    pullPolicy: IfNotPresent
  postgres:
    repository: postgres
    tag: 16-alpine
    pullPolicy: IfNotPresent
  keycloak:
    repository: quay.io/keycloak/keycloak
    tag: 25.0.6
    pullPolicy: IfNotPresent

service:
  app:
    type: ClusterIP
    port: 80
    targetPort: 3000
  postgres:
    type: ClusterIP
    port: 5432
  keycloak:
    type: ClusterIP
    port: 8080
  keycloakPostgres:
    type: ClusterIP
    port: 5432

app:
  port: 3000
  nodeEnv: production
  publicUrl: http://localhost:3000
  database:
    secretName: metrics-server-database
    secretKey: database-url
  auth:
    clientId: todo-app
    issuerUrl: http://localhost:8081/realms/todos
    authorizeUrl: http://localhost:8081/realms/todos/protocol/openid-connect/auth
    tokenUrl: http://localhost:8081/realms/todos/protocol/openid-connect/token
    logoutUrl: http://localhost:8081/realms/todos/protocol/openid-connect/logout
    redirectUri: http://localhost:3000/auth/callback
    postLogoutRedirectUri: http://localhost:3000/login
    scope: openid profile email
  email:
    smtp:
      host: ""
      port: 587
      user: ""
      pass: ""
      secure: false
      from: ""
    reminderIntervalMs: 3600000

secrets:
  create: false
  databaseUrl: ""
  postgresPassword: ""
  keycloakPostgresPassword: ""
  keycloakAdminPassword: ""

externalSecrets:
  enabled: false
  apiVersion: external-secrets.io/v1
  refreshInterval: 1h
  secretStoreRef:
    name: cluster-secret-store
    kind: ClusterSecretStore
  database:
    remoteRef:
      key: metrics-server/database
      property: database-url
  postgresPassword:
    remoteRef:
      key: metrics-server/postgres
      property: postgres-password
  keycloakPostgresPassword:
    remoteRef:
      key: metrics-server/keycloak-postgres
      property: postgres-password
  keycloakAdminPassword:
    remoteRef:
      key: metrics-server/keycloak
      property: admin-password

postgres:
  enabled: true
  auth:
    username: todouser
    database: tododb
    passwordSecretName: metrics-server-postgres
    passwordSecretKey: postgres-password
  persistence:
    enabled: true
    size: 1Gi
    storageClassName: ""

keycloak:
  enabled: true
  realm: todos
  clientId: todo-app
  publicUrl: http://localhost:8081
  hostname: localhost
  auth:
    username: admin
    passwordSecretName: metrics-server-keycloak
    passwordSecretKey: admin-password
  ingress:
    enabled: false
    className: ""
    host: localhost
    annotations: {}
    tls: []
  database:
    username: keycloak
    database: keycloak
    passwordSecretName: metrics-server-keycloak
    passwordSecretKey: postgres-password
  persistence:
    enabled: true
    size: 1Gi
    storageClassName: ""

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: localhost
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources: {}

podAnnotations: {}

nodeSelector: {}

tolerations: []

affinity: {}
